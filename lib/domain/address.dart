class Address {
  Address({this.id, this.firstName, this.lastName, this.country, this.province, this.district, this.phone, this.postCode});

  String? id, firstName, lastName, country, province, district, phone, postCode;
}
